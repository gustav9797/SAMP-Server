#include <iostream>
#include <map>
#include <string>
#include "Interior.h"

class Interiors
{
	std::map<std::string, Interior> *interiorList = new std::map<std::string, Interior>();

	*Interior GetInterior(std::string name)
	{

	}
};